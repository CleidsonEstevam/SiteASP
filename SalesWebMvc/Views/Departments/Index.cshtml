@model IEnumerable<SalesWebMvc.Models.Department>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create"class="btn btn-default" style="background-color: #df691a">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id"class="btn-default" style="background-color: #df691a">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id"class="btn-default" style="background-color: #df691a">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id"class="btn-default" style="background-color: #df691a">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
